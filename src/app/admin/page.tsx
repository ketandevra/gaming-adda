import { AdminPanelClient } from "./AdminPanelClient";

export const metadata = {
  title: "View Bookings — The Gaming Adda",
  description: "View and manage all bookings",
};

export default function AdminPage() {
  return <AdminPanelClient />;
}
